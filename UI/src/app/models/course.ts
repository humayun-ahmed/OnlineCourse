export class Course {
  courseGuid?: string;
  name: string;
  teacher?: string;
  maxParticipants: number;
  lastUpdated: Date;
  totalParticipants: number;
  // constructor(id: number, name: string) {
  //   this.id = id;
  //   this.name = name;
  // }
}

